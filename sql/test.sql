use challenge_linio_db;

/**
 * MYSQL DOESN'T implement nothing for Unit Testing
 *
 * But i wrote this algorithm in Procedural SQL (MYSQL)
 *
 * Just Print first 100 numbers and result is stored in table _results
 */

call FizzBuzzLinio(100);
call PrintResults();